rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // User profiles: Only the owner can read/write
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;

      // Properties and their sub-resources nested under users
      match /properties/{propertyId} {
        
        function isLandlord() {
          return isSignedIn() && request.auth.uid == userId;
        }

        function isMember() {
          return isSignedIn() && (
            isLandlord() ||
            (resource != null && resource.data.members != null && resource.data.members[request.auth.uid] != null)
          );
        }

        // Direct collection access for the landlord
        allow list, get, create, update, delete: if isLandlord();
        
        // Members (tenants) can get the property
        allow get: if isMember();

        // Sub-collections nested under properties
        match /tenantProfiles/{id} { 
          allow read: if isLandlord() || (isSignedIn() && (resource.data.userId == request.auth.uid));
          allow write: if isLandlord(); 
        }
        match /images/{id} { 
          allow read: if isLandlord() || isMember(); 
          allow write: if isLandlord(); 
        }
        match /documents/{id} { 
          allow read: if isLandlord() || isMember() || (isSignedIn() && resource.data.userId == request.auth.uid); 
          allow write: if isLandlord(); 
        }
        match /inspections/{id} { 
          allow read: if isLandlord() || isMember(); 
          allow write: if isLandlord(); 
        }
        match /maintenanceRequests/{id} { 
          allow read: if isLandlord() || (isSignedIn() && (resource.data.tenantId == request.auth.uid || resource.data.landlordId == request.auth.uid)); 
          allow create: if isSignedIn();
          allow update, delete: if isLandlord() || (isSignedIn() && resource.data.tenantId == request.auth.uid);
        }
        match /emergencyContactSheets/{id} { 
          allow read: if isLandlord() || isMember(); 
          allow write: if isLandlord(); 
        }
        match /emergencyContacts/{id} { 
          allow read: if isLandlord() || isMember(); 
          allow write: if isLandlord(); 
        }
      }
    }

    // Top-level maintenanceRequests for cross-property tenant access
    match /maintenanceRequests/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.reportedByUserId == request.auth.uid
      );
      allow create: if isSignedIn();
      allow update: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.reportedByUserId == request.auth.uid
      );
    }

    // Top-level tenants (Tenant Profiles) mapping
    match /tenants/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.userProfileId == request.auth.uid
      );
      allow write: if isSignedIn() && resource.data.landlordId == request.auth.uid;
    }

    // Collection Group rules for cross-property queries
    match /{path=**}/properties/{id} {
      allow list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        (resource.data.members != null && resource.data.members[request.auth.uid] != null)
      );
    }
    match /{path=**}/maintenanceRequests/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.tenantId == request.auth.uid ||
        resource.data.reportedByUserId == request.auth.uid
      );
    }
    match /{path=**}/inspections/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        (resource.data.members != null && resource.data.members[request.auth.uid] != null)
      );
    }
    match /{path=**}/tenantProfiles/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.userId == request.auth.uid
      );
    }
    match /{path=**}/documents/{id} {
      allow read, list: if isSignedIn() && (
        resource.data.landlordId == request.auth.uid || 
        resource.data.userId == request.auth.uid ||
        (resource.data.members != null && resource.data.members[request.auth.uid] != null)
      );
    }
  }
}
